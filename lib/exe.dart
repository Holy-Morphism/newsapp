import 'package:flutter/material.dart';

class MyWigdet extends StatelessWidget {
  const MyWigdet({
    super.key,
  });
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('wigdet'),
    );
  }
}
